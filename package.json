{
  "name": "midi-techno-lights",
  "version": "0.1.0",
  "description": "A command-line app that makes MIDI messages control flashing LED lights.",
  "main": "index.js",
  "scripts": {
    "start": "./bin/controller",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dave Jensen and Andrew Jensen",
  "license": "MIT",
  "dependencies": {
    "inquirer": "^1.1.3",
    "midi": "^0.9.5",
    "node-hue-api": "^2.2.0"
  },
  "devDependencies": {
    "mocha": "^3.0.2"
  }
}
